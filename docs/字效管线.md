# 墨弹防线 · 字效管线

> 加字只填定义，不新开一套 if/else。26 字全部挂在这套槽位上，新字对号入座。
> 玩法锁：一字一牌；常驻 / 蓄力 / 词组；三行一列；母弹进列计数；成词后常驻仍可叠。
> 本文是**实现后的回写**，第 5 / 6 节的数字就是 `Assets/Scripts/Data/GlyphDef.cs` 里的值，改代码要同步改这里。

## 1. 总原则

1. **效果按槽位叠，不按「这对元素有没有专图」叠。** 同槽取强（Max / 后写覆盖），异槽全保留。
2. **伤害是一条管子，状态是另一条管子。** 「本身无伤害」只表示这个字不改伤害数字，不阻止它给弹上状态。
3. **表现也是槽位。** 炮弹最多同时亮 5 层：体 / 尾 / 环 / 绕 / 晕光。颜色可以叠，形不能抢同一层。
4. **两两三三不画专图。** 8 元素两两 28、三三 56，小游戏养不起。新鲜感来自层叠，只给 12 对「招牌两两」换一张体图。
5. **星级：★1 身份，★2 加数，★3 才允许多一个动词。** 冰（缓→冻）就是标准。不要每个字 ★3 都变一个新玩法。

## 2. 五个家族

| 家族 | 管的是 | 字 |
|---|---|---|
| 伤 Hurt | 数字怎么算 | 金（加算）、重（乘算）、木（吸血） |
| 状 Status | 打上以后留在人身上 | 火、毒、冰、水、雷、晕、惑 |
| 位 Move | 把人挪开 | 土（沿列）、风（换列）、击退（词） |
| 道 Ballistic | 弹怎么飞 | 分、瞄、速、穿 |
| 域 Area | 这一击波及谁 | 炸、万箭（词）、连斩（词） |

一个字只主属一个家族，可以带一个「骑手」（火★2 击杀掉小爆，冰★3 带冻）。骑手不能再开第三家族。

## 3. 命中结算十步（所有字共用）

直击、溅射、连斩、落箭都进 `BattleWorld.ResolveHit(e, mods, src, from)`，`from` 区分来源。
溅射 / 万箭 **关掉斩杀**，避免一圈全斩；域伤害不再触发域，避免自激。

```
0  盾：普通弹只破盾就停；斩杀破盾后继续
1  斩杀（最高，仅 Direct）：非头目 HP→0；头目按最大生命比例
2  基础弹伤 × 道族衰减（穿透每穿一个 ×0.85，分裂每股 ×0.6，溅射 ×0.6，连斩每跳 ×0.75）
3  伤族加算池求和（金）
4  伤族乘算池连乘（重，最后）
5  木：不改打出的血，把结算总量按比例记入吸血池
6  扣 HP
7  状族：灼烧 / 毒 / 缓 / 冻 / 晕 / 惑
8  位族：土沿列上推；风横向换列
9  域族：炸圈、连斩跳、万箭落（不再走斩杀，也不再触发域）
```

**第 3 / 4 步的顺序是 `伤 × 乘算 + 加算`，加算默认不吃乘算。** 这条不是笔误：如果加算天然吃乘算，招牌「镇金」（金+重）就没有内容了。镇金亮起时才变成 `(伤 + 加算) × 乘算`。

### 六条叠加规则

1. **加算先求和，乘算最后连乘。** 两个池互不干涉。
2. **状态同名取强**（DPS 与时长各自 Max，减速取移速系数的 Min）。**毒是唯一可叠层的**，层数上限吃字的星级。
3. **硬控同时只生效一个**，优先级 `冻(3) > 晕(2) > 惑(1)`。输的那个降级成等时长的「缓」（移速 ×0.5），不白打。见 `GlyphTable.HardRank`。
4. **位移同轴取最大，异轴都执行。** 土和击退都是沿列，取远的那个；风是横向，和它们同时生效。
5. **域伤害不再触发域。** 炸圈里打死的人不会再炸一次。
6. **12 张招牌两两只改表现 + 一条小加成**，不动上面这十步。

晕自带防连晕：一次晕结束后 `时长 + 0.35s` 内免疫下一次硬控。

## 4. 四类表现规格

### 4.1 炮弹五槽（`ShotLook.Resolve` → `InkShot`）

| 槽 | 谁占用 | 叠法 | 优先级（高→低） |
|---|---|---|---|
| 体 Form | 元素主形，一发只一张 | 取优先级最高 | 秒杀 > 连斩 > 击退 > 万箭 > 炸 > 火 > 冰 > 毒 > 水 > 土 > 穿 |
| 尾 Trail | 道族、风 | 最多 2 条，颜色并行 | 速 > 瞄 > 风 |
| 环 Halo | 晕、雷、金 | 取后写，半透明 | 晕 > 雷 > 金 |
| 绕 Orbit | 风、木 | 取后写 | 风 > 木 |
| 晕光 Bloom | 所有上色字 | 颜色永远混合 | 重 > 金 |

赢下「体」槽的那个字，颜色不是纯色，而是 `Lerp(该字主色, 全部元素混色, 0.35)` —— 这样被压掉的元素在弹身上仍然看得出来。
体槽有图时**隐藏底下的黑色弹丸**，否则会出现「彩色包黑心」。
重只放大视觉（`Size` 1.15 / 1.35 / 1.6），**不动碰撞半径**。

### 4.2 命中三层（`FxBurst` → `InkBurst.Play`）

一次命中固定播三层，不叠三套爆炸：

1. **墨溅底**：始终有，白墨四帧，按混色 tint。
2. **元素层**：只播一个元素的四帧，优先级 `秒杀 > 域 > 火 > 冰 > 雷 > 毒 > 水 > 土 > 风 > 金 > 木`。被压掉的元素退化成**边上的色点**（最多 2 颗），所以三元素弹的命中仍然三色可辨。
3. **事件层**：`Kill / Stun / Push / Pierce` 单帧印章，没有事件就不出。

命中停顿（hitstop）只由事件层和重决定，元素层不产生停顿。

### 4.3 敌人身上的 DoT overlay（`InkDot.Sync`）

每种状态一颗标记，互不删，挂在敌人贴图上并**反向抵消敌人的呼吸缩放**，否则会跟着一起胀缩：

| 状态 | 标记 | 位置 | 说明 |
|---|---|---|---|
| 灼烧 | 火苗 | 头顶 | 抖动，强度随 DPS |
| 毒 | 气泡 | 头顶偏侧 | **按层数出 1～4 颗** |
| 缓 | 水纹 | 脚下 | 横向压扁 |
| 冻 | 冰壳 | 全身 | 覆盖整体，青色 |
| 晕 | 金环 | 头顶 | 绕圈转 |
| 惑 | 紫漩涡 | 头顶 | 反向慢转 |

**与计划的偏差：** 计划写的是「一张 512² 图集 + tint」。实际做成了 `DotMarks` 在运行时烘 64² 的火苗 / 水纹 / 漩涡三张基图，靠 tint + 变换派生出全部六种。省掉一张图集、省掉 512² 的显存，tint 驱动的部分完全一致。要换成真图集时只改 `DotMarks`，`InkDot.Sync` 不动。

### 4.4 抽卡字面（`InkArt.OnCard`）

字图要在 CPU 上套宣纸卡框，所以 `Assets/Resources/Art/heap_*.png` 必须 `isReadable` 且不压缩；`Assets/Resources/Art/Vfx/` 下的只当贴图用，可读关掉、压缩打开。分流在 `Assets/Editor/InkArtImporter.cs`。

## 5. 单字全表

数值来源：`Assets/Scripts/Data/GlyphDef.cs`。基础弹伤 2.4，弹速 7.2，射击间隔 0.34s。

### 伤族

| 字 | 触发 | ★1 | ★2 | ★3 | 槽 |
|---|---|---|---|---|---|
| 金 | 常驻 | 加算 +1.2 | +2.2 | +3.6 | 环金 + 晕光金 |
| 重 | 蓄 2 | 乘算 ×1.6 | ×1.9 | ×2.3 | 晕光，视觉放大 1.15/1.35/1.6 |
| 木 | 常驻 | 总伤 12% 入吸血池，每波上限 1 | 20% / 2 | 30% / 3 | 绕淡绿 |

吸血池攒满 1 点就回 1 基地血，每波有上限、跨波清零，基地血不超过 3。

### 状族

| 字 | 触发 | 状态 | ★1 | ★2 | ★3 | 槽 |
|---|---|---|---|---|---|---|
| 火 | 常驻 | 灼烧 | DPS 1.6 / 3s | 2.8，击杀掉小爆 | 4.4 | 体火舌 |
| 毒 | 常驻 | 中毒 | DPS 1.0 / 6s，2 层 | 1.7 / 3 层 | 2.6 / 4 层 | 体毒滴 |
| 冰 | 常驻 | 缓（★3 可冻） | 移速 ×0.72 / 2s | ×0.55 | ×0.38，35% 几率冻 0.45s | 体霜滴 |
| 水 | 常驻 | 范围缓 | ×0.85 / 1.5s，半径 0.6 | ×0.75 | ×0.62 | 体水花 |
| 雷 | 常驻 | 晕（范围） | 0.5s，半径 0.5，连带 1 | 0.75s / 0.7 / 2 | 1.0s / 0.9 / 3 | 环电 |
| 晕 | 蓄 2 | 晕（单体） | 0.5s | 0.75s | 1.0s | 环金 |
| 惑 | 常驻 | 惑 | 3s，输出降 30% | 4s | 5s | —（靠命中层和 DoT 标记） |

火高 DPS、短、不可叠；毒低 DPS、长、可叠层——两者是独立的两条 DoT，不抢同一个字段。
雷常驻、范围、短控；晕蓄力、单体、同时长——雷不额外加伤，靠连带铺开。惑对头目无效。

### 位族

| 字 | 触发 | 轴 | ★1 | ★2 | ★3 |
|---|---|---|---|---|---|
| 土 | 常驻 | 沿列上推 | 0.5 格 + 0.4s 落地硬直 | 0.8 格 | 1.1 格 |
| 风 | 常驻 | 横向换列 | 1 列 | 2 列 | 2 列 + 0.3s 硬直 |

落地硬直不算硬控，和冻 / 晕 / 惑不互斥。

### 道族

| 字 | 触发 | ★1 | ★2 | ★3 |
|---|---|---|---|---|
| 分 | 常驻 | 裂 2，每股 ×0.6 | 裂 3 | 裂 4 |
| 瞄 | 常驻 | 转向 2.4 | 4.2 | 6.4 |
| 速 | 常驻 | 弹速 ×1.18 | ×1.34 | ×1.5 |
| 穿 | 蓄 2 | 穿 1，每穿一个 ×0.85 | 穿 2 | 穿 3 |

### 域族

| 字 | 触发 | ★1 | ★2 | ★3 |
|---|---|---|---|---|
| 炸 | 蓄 2 | 半径 0.55，圈内分摊 60% | 0.82 | 1.12 |

### 词组字

| 词 | 蓄 | ★1 | ★2 | ★3 |
|---|---|---|---|---|
| 秒杀 | 3 | 非头目 HP ≤ 35% 直接死；头目按最大生命 22% | 45% / 30% | 60% / 40% |
| 万箭 | 4 | 同列落 3 箭，每箭 2.2 | 4 箭 / 3.2 | 6 箭 / 4.4 |
| 击退 | 2 | 上推 0.55 格 | 0.85 | 1.2 |
| 连斩 | 3 | 跳 1，每跳 ×0.75 | 跳 2 | 跳 3 |

三行里两个词组仍然只能醒一个。

## 6. 12 张招牌两两

数据在 `Assets/Scripts/Data/SignaturePairs.cs`，一次性调值在 `SignaturePairs.Tune`，第一次命中前跑一次。
每对**只换「体」槽一张图 + 一条小加成**，不新增结算步骤。三合四合没有专属内容，照常走五槽叠色。

| 组合 | 名 | 小加成 |
|---|---|---|
| 火+冰 | 霜火 | 灼烧 DPS +25% |
| 火+雷 | 焦雷 | 晕结束时补一次「灼烧 DPS ×2」的伤害 |
| 冰+雷 | 霰雷 | 打在已冻结的目标上算碎冰，本次伤害 +40% |
| 火+毒 | 燎毒 | 毒层数上限 +1 |
| 水+雷 | 导电 | 雷连带数 +1，范围 +30% |
| 金+火 | 熔金 | 灼烧改为按目标最大生命 1%/s 计（取高者） |
| 金+重 | 镇金 | 加算部分也吃乘算 |
| 木+毒 | 蚀生 | 毒造成的伤害 100% 计入吸血池 |
| 土+重 | 夯土 | 上推距离 +50% |
| 风+冰 | 寒风 | 横移后附带一层缓 |
| 炸+火 | 烈爆 | 爆圈内追加 1.5s、半程 DPS 的灼烧 |
| 斩+雷 | 雷决 | 斩杀阈值 +8% |

**与计划的偏差（烈爆）：** 计划写「爆圈里留 1.5s 火地」。实际做成了爆炸瞬间给圈内所有人补一层 1.5s 的半程灼烧。差别只在「之后走进来的敌人不吃」。做真火地需要一套持续区域实体 + 对应表现，等有专图再补；换过去时只改 `BattleWorld.Explode` 里那三行。

体槽专图还没画的对，`ShotLook.Fallback` 会退回该对里主元素的现成图，不会出现空白弹。

## 7. 新元素的投放节奏

`Assets/Scripts/Data/StageCatalog.cs` 的 `ChapterUnlocks` 按章解锁，每章最多进两个新家族：

| 章 | 新增 | 补的是什么 |
|---|---|---|
| 一 | —（底盘 18 字） | — |
| 二 | 金、雷 | 加伤 + 范围控 |
| 三 | 木、毒 | 吸血 + 可叠层 DoT |
| 四 | 水、土 | 范围缓 + 上推 |
| 五 | 风、惑 | 横移 + 反打 |

关卡内容目前只有第一章八关，所以二章以后的池子已经数据化但还没有关卡挂上去。要在编辑器里看新元素，用战斗内的调试预览页（共 7 页，后三页分别是金木水土 / 风雷毒惑 / 霜火·焦雷·镇金·蚀生）。

**待观察的平衡点：** 雷和惑都是常驻无 CD 的硬控。防连晕（时长 + 0.35s 免疫）和头目免疫惑挡住了最坏情况，但如果实测发现常驻范围晕过强，就把雷改成蓄力，走和「晕」同一套充能通道。

## 8. 贴图预算

| | 之前 | 现在 |
|---|---|---|
| 磁盘 | 10.1 MB | 6.0 MB |
| 运行时贴图 | ~214 MB | 35.6 MB |

做了五件事：按目录分流导入设置、全部限到 256²、把压黑挪到离线、字图母版降到 256²、删掉 39 张早期按星级切图的遗留 `heap_*`（64 → 25）。

现在的 35.6 MB 拆开看：

| 分组 | 占用 | 为什么这么贵 |
|---|---:|---|
| 敌人 + 炮 + UI 图标（16 张） | 18.6 MB | 一刀切成了可读且不压缩，8 字节/像素 |
| `heap_*` 字图（25 张） | 12.5 MB | `InkArt.OnCard` 要在 CPU 上套卡框，必须可读不压缩 |
| `Vfx`（94 张） | 4.6 MB | 已压缩且不可读，很便宜 |

最大头已经不是字图，而是敌人和图标。其中真正需要可读的只有 7 张敌人图 —— `InkSprites.Flash` 靠 `GetPixels` 烘白色受击帧。炮和 9 张图标根本没人读像素，关掉可读能省 2.0 MB，但要在导入器里硬写文件名，加敌人时会静默踩坑，所以没做。**正解是把 `Flash` 改成材质染色而不是 `GetPixels`，那样 6.6 MB 一次全释放**，和字图的离线烘卡框是同一类改造。

`Resources/` 是整包打进去的，不要往里丢没有代码引用的素材。现在 `Resources/Art/Vfx/` 下 94 个文件里只有 13 个被代码引用，其余是没清理的试验产物。

## 9. 工具

| 脚本 | 干什么 |
|---|---|
| `docs/prompt/runtime/vfx_crush.py` | 离线压黑 + 限尺寸，母版存 `art_src/vfx/shots_black/`（在 `Assets/` 外，不会被打包） |
| `docs/prompt/runtime/heaps_downscale.py` | 把 `heap_*.png` 降到 256²，只在仓库外有同名母版时才动 |
| `docs/prompt/runtime/csharp_check.sh` | 不开编辑器直接用团结自带的 Roslyn 编译校验 runtime / editor 两个程序集 |

`csharp_check.sh` 有两个坑已经踩过：不要引 `Managed/UnityEngine.dll` 和 `Managed/UnityEditor.dll` 这两个门面（和拆分模块类型重名，报 CS0433）；UI 是包不是内置，要引 `Library/ScriptAssemblies/UnityEngine.UI.dll`。

## 10. 加一个字要填的定义

复制这一段就能开做。缺一项就先别写代码。

```
字：
家族：伤 / 状 / 位 / 道 / 域   （只能选一个主家族）
触发：常驻 / 蓄力(过N发) / 词组(伴侣字, 过N发)
本身加伤：无 / 加算 / 乘算 / 斩杀
状态 id：无 / burn / poison / slow / freeze / stun / confuse
状态叠法：Max 数值、Max 时间、独立条（毒和火必须独立）
位移轴：无 / 沿列 / 换列
弹道：无 / 裂 / 追 / 加速 / 穿
范围：无 / 圈 / 跳 / 同列点名
星级：★1 身份　★2 数字　★3 多一个动词？(是/否，动词是什么)
炮弹槽：体 / 尾 / 环 / 绕 / 晕光   （标明占用哪几槽、色）
命中元素层：沿用哪一种 HitFx
DoT 标记：用哪颗、挂哪个位置
和谁同槽会互斥：
溅射是否继承：斩杀默认否；灼烧/缓默认是
```

代码落点，不要在别处加长 if：

- `GlyphDef.cs`：这份定义本身，加一行 `new GlyphDef { ... }`
- `ShotMods.cs`：弹上只挂槽位和数值，不挂字名
- `BattleWorld.ResolveHit`：只读 `ShotMods`，按第 3 节十步走
- `ShotLook.cs`：五槽优先级数组，加字就往数组里插一个 id
- `InkShot.cs` / `InkDot.cs` / `InkFx.cs`：只读槽位，不读 `CardId`

## 11. 明确不做

- 不为每对元素画一张弹皮，招牌两两封顶 12 张体图。
- 不把金木水火土风雷冰做成第二套 18 字替换现在的牌。
- 不让「本身无伤害」变成「这发变成 0 伤」（炮还是有基础伤）。
- 不让木直接每发回满基地血。
- 三行里两个词组仍然只能醒一个。
- 不在 `Resources/` 里堆没有代码引用的素材。
